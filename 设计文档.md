# 项目 1：个人助理 Bot 设计文档

## 1. 项目目标

实现一个"网页聊天 + 工具调用"的个人助理 Bot：

- 浏览器里可以和助手聊天
- 助手内部运行 ReAct 循环，能够调用天气、计算器、本地笔记搜索三类工具
- 工具调用过程以折叠卡片形式展示给用户
- 支持流式输出，呈现逐字打字效果
- 可用一条命令分别启动后端和前端，本地一键跑通

## 2. 技术选型

| 层 | 技术 | 说明 |
| --- | --- | --- |
| Python 环境 | `uv` | 用 `uv` 管理 Python 版本、虚拟环境和依赖 |
| 后端框架 | `FastAPI + uvicorn` | REST 接口 + WebSocket，Pydantic 做参数校验 |
| 大模型 | DeepSeek API | OpenAI 兼容接口，支持流式输出 |
| 前端 | `React + TypeScript + Vite` | 主流 React 工程方案 |
| 样式 | Tailwind CSS | 快速搭建聊天界面；也可用 Ant Design |
| 测试 | `pytest` | 后端工具和接口测试 |

## 3. 总体架构

```text
浏览器 React 前端
   │  HTTP  /  WebSocket
   ▼
FastAPI 后端
   │
   ├── POST /chat          非流式聊天
   ├── GET  /health        健康检查
   └── WS   /ws/chat       流式聊天
          │
          ▼
     ReAct Agent 循环
     ├── 模型输出 JSON（thought / action / action_input）
     ├── 执行注册表里的工具
     ├── Observation 回传给模型
     └── 最多 5 轮，输出 Final Answer
          │
          ▼
     DeepSeek API（OpenAI 兼容）
```

## 4. 目录结构

```text
project-1-personal-assistant/
├── backend/                     # Python 后端（uv 管理）
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py              # FastAPI 入口、CORS、路由挂载
│   │   ├── schemas.py           # Pydantic 请求/响应模型
│   │   ├── config.py            # 读取 .env：API Key、模型名等
│   │   ├── llm.py               # DeepSeek 客户端封装（非流式 + 流式）
│   │   ├── agent.py             # ReAct 主循环
│   │   ├── tools.py             # 工具注册表与参数校验
│   │   └── ws.py                # WebSocket 流式转发逻辑
│   ├── tests/                   # pytest 测试
│   ├── notes/                   # search_notes 搜索的本地 md 目录
│   ├── .env.example             # API Key 模板
│   ├── pyproject.toml           # uv 项目配置
│   └── .python-version          # 固定 Python 版本
├── frontend/                    # React 前端
│   ├── src/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   ├── components/
│   │   │   ├── ChatMessage.tsx      # 用户/助手气泡
│   │   │   ├── ToolCallCard.tsx     # 工具调用折叠卡片
│   │   │   └── MessageList.tsx
│   │   ├── hooks/
│   │   │   └── useChatSocket.ts     # WebSocket 连接、断线重连
│   │   └── api/
│   │       └── client.ts            # fetch 封装
│   ├── index.html
│   ├── package.json
│   └── vite.config.ts              # dev proxy，避免 CORS
└── README.md                       # 架构图、启动命令、截图、复盘
```

## 5. uv 搭建后端工程

### 5.1 初始化

```bash
mkdir backend && cd backend
uv init --python 3.12
uv add fastapi "uvicorn[standard]" pydantic openai python-dotenv
uv add --dev pytest pytest-asyncio
uv lock
```

说明：

- `uv init --python 3.12` 会生成 `pyproject.toml`、`.python-version` 和最小工程骨架
- `uv add` 自动创建 `.venv` 并安装依赖，不需要手动 `pip install`
- 环境变量统一放 `.env`，由 `python-dotenv` 加载；`config.py` 中读取 `DEEPSEEK_API_KEY`

### 5.2 常用命令

```bash
uv run uvicorn app.main:app --reload --port 8000   # 开发启动
uv run pytest                                      # 跑测试
uv add fastapi                                    # 新增依赖
uv sync                                            # 按 lock 文件同步环境
```

## 6. 核心模块设计

### 6.1 DeepSeek 客户端

- 使用 OpenAI SDK，配置 `base_url="https://api.deepseek.com"`
- 默认模型：`deepseek-chat`
- 提供两个方法：
  - `chat(messages)`：非流式，用于工具决策
  - `stream(messages)`：流式，逐块 yield 文本给 WebSocket
- system prompt 固定描述工具格式：只输出 JSON，包含 `thought`、`action`、`action_input` 或 `final_answer`

### 6.2 工具注册表

采用"名称 -> 函数"的字典注册方式：

```python
TOOLS = {
    "get_weather": get_weather,
    "calculator": calculator,
    "search_notes": search_notes,
}
```

三个工具：

| 工具 | 功能 | 实现要点 |
| --- | --- | --- |
| `get_weather` | 查询城市天气 | 先用 mock 数据保证离线可跑，预留真实天气 API 接入点 |
| `calculator` | 四则运算 | 用 `ast` 解析表达式，禁止 `eval` |
| `search_notes` | 搜索本地 md 笔记 | 遍历 `notes/`，按关键词匹配文件名和内容 |

所有工具统一返回字符串或可 JSON 序列化的字典，统一作为 Observation 回传给模型。

### 6.3 ReAct 主循环

```text
输入用户消息
  │
  ▼
构造 messages（system + 历史 + 用户问题）
  │
  ▼
调用 DeepSeek，解析 JSON
  ├── 有 final_answer ──► 返回最终回答，结束
  ├── 有 action ────────► 查注册表
  │     ├── 工具存在：执行，得到 Observation
  │     └── 工具不存在：Observation = "未知工具"
  │
  ├── JSON 解析失败 ────► 提示模型重新输出，重试 1 次
  │
  └── 轮数 >= 5 ───────► 返回兜底回答，结束
```

容错设计：

- 轮数上限：5
- 单次工具调用超时：5 秒
- 总请求超时：30 秒
- 工具抛异常：捕获后转为 `Observation: 工具执行失败: xxx` 回传模型，不中断会话
- 前端超时：浏览器端 60 秒无响应则提示用户重试

## 7. API 设计

### 7.1 `GET /health`

返回：

```json
{"status": "ok"}
```

### 7.2 `POST /chat`（非流式）

请求：

```json
{
  "message": "北京今天天气怎么样？",
  "history": []
}
```

响应：

```json
{
  "reply": "北京今天晴，22 度。",
  "trace": [
    {
      "step": 1,
      "thought": "用户想查天气，需要调用天气工具",
      "tool": "get_weather",
      "input": "北京",
      "result": "晴，22 度"
    }
  ]
}
```

### 7.3 `WS /ws/chat`（流式）

客户端发送：

```json
{"type": "message", "content": "北京今天天气怎么样？"}
```

服务端按顺序发送以下事件：

```json
{"type": "tool_call", "tool": "get_weather", "input": "北京"}
{"type": "tool_result", "tool": "get_weather", "result": "晴，22 度"}
{"type": "token", "content": "北京"}
{"type": "token", "content": "今天"}
{"type": "done"}
{"type": "error", "message": "服务端超时"}
```

前端根据 `type` 分别渲染：工具事件生成折叠卡片，`token` 事件逐字追加到助手气泡。

## 8. 前端设计

### 8.1 工程初始化

```bash
mkdir frontend && cd frontend
pnpm create vite . --template react-ts   # 或 npm create vite@latest
pnpm add tailwindcss                      # 按 Tailwind v4 方式配置
```

### 8.2 页面结构

- 顶部：项目标题 + 连接状态
- 中部：消息列表（用户消息靠右，助手消息靠左）
- 工具卡片：折叠在助手消息上方或下方，展示工具名、参数、结果
- 底部：输入框 + 发送按钮，Enter 发送，Shift+Enter 换行

### 8.3 核心组件

- `useChatSocket`：管理 WebSocket 生命周期、重连（指数退避）、消息状态机
- `ChatMessage`：渲染气泡，支持 markdown 简单渲染
- `ToolCallCard`：折叠卡片，展开显示工具输入和输出
- `TypingIndicator`：等待回复时的打字指示

### 8.4 流式体验

- 收到 `token` 事件后追加到当前助手消息
- 收到 `done` 后结束流式状态，允许发送下一条
- 断线时自动重连，重连成功提示"连接已恢复"
- 流式过程中禁用发送按钮，避免消息顺序混乱

## 9. 开发实施计划

| 阶段 | 任务 | 验收标准 |
| --- | --- | --- |
| Day 1 | ReAct 最小循环：JSON 输出、工具执行、5 轮上限 | 命令行跑通"查天气 -> 回答"；画出 ReAct 流程图 |
| Day 2 | 工具注册表 + calculator + search_notes + 异常回传 | 3 个工具可用，工具报错不崩溃 |
| Day 3 | FastAPI 路由、Pydantic、CORS、`/health` | `POST /chat` 可通过 `requests` 调用 |
| Day 4 | React 聊天界面 + 工具折叠卡片 | 浏览器可聊天，能看到工具调用过程 |
| Day 5 | WebSocket 流式 + 断线重连 | 前端逐字渲染，断线自动恢复 |
| Day 6 | 端到端测试、README、复盘、面试题 | 连续问 10 个问题无阻断性 bug；项目推到 GitHub 并置顶 |

## 10. 一键启动流程

后端：

```bash
cd backend
uv run uvicorn app.main:app --reload --port 8000
```

前端：

```bash
cd frontend
pnpm dev
```

浏览器访问 `http://localhost:5173`。开发阶段也可以用 Vite proxy 把 `/api` 和 `/ws` 转发到 `localhost:8000`，减少 CORS 配置。

## 11. 测试与验收

后端测试：

- `test_calculator.py`：正常计算、非法表达式、除零
- `test_search_notes.py`：命中、无命中、目录不存在
- `test_agent.py`：mock DeepSeek 响应，验证循环终止和轮数上限
- `test_api.py`：`/health`、`/chat`、WebSocket 连通性

端到端验收：

1. 启动后端和前端
2. 连续问 10 个不同问题，覆盖天气、计算、笔记搜索、普通聊天
3. 断网 / 重启后端，验证前端提示和重连
4. 截图聊天界面，放入 README

## 12. README 与 GitHub

README 包含：

- 项目简介 + 截图
- 架构图（Mermaid 或图片）
- 环境要求（Python 3.12、uv、Node、pnpm）
- 启动命令
- API 说明
- 面试题复盘：ReAct 循环、Agent vs 普通 LLM API 调用

GitHub：

- 新建仓库并推送
- `.gitignore` 排除 `.venv`、`.env`、`node_modules`、`dist`
- 项目 1 设为置顶仓库

## 13. 面试题准备要点

### 什么是 ReAct

ReAct 是 Reasoning + Acting 的组合：模型在推理（Thought）和行动（Action）之间交替，每次行动后观察环境反馈（Observation），再继续推理，直到给出最终答案（Final Answer）。

### Agent 和单纯 LLM API 调用的区别

- 普通调用：一次请求一次响应，模型只有对话历史，不能改变外部世界
- Agent：模型可以调用工具、读取工具结果、多轮推理，能访问外部信息并持续修正回答
- Agent 的核心是循环：推理 -> 行动 -> 观察 -> 再推理，普通 LLM API 调用只是这个循环中的一步

## 14. 风险与应对

| 风险 | 应对 |
| --- | --- |
| DeepSeek 输出不是合法 JSON | 宽松提取 `{}` 内容解析，失败重试 1 次 |
| ReAct 陷入死循环 | 轮数上限 5 + 总超时 |
| 工具执行崩溃 | 统一 try/except，错误转成 Observation |
| 流式与工具事件顺序混乱 | 用消息类型 + step 序号保证渲染顺序 |
| CORS / 端口冲突 | 固定 8000/5173，开发期用 Vite proxy |
| API Key 泄露 | 只放 `.env`，提交 `.env.example`，GitHub 仓库忽略 `.env` |

